// Require util and fs built in features of node
const util = require('util');
const fs = require ('fs');

const uuidv1 = require ('uuid/v1');


//writeToFile
//readToFile

// Require uuid/v1 package in package.json
class Store {
    read() {

    }

    write() {

    }

    //funcionality to get, add and delete notes
}

module.exports = new Store();